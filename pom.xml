<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	
    <groupId>com.xhzb</groupId>
    <artifactId>xhzb</artifactId>
    <version>3.9.0</version>

    <name>xhzb</name>
    <url>http://www.xhzb.com</url>
    <description>星海智伴后台管理系统</description>
    
    <properties>
        <xhzb.version>3.9.0</xhzb.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>17</java.version>
        <maven-jar-plugin.version>3.1.1</maven-jar-plugin.version>
        <spring-boot.version>3.5.0</spring-boot.version>
        <druid.version>1.2.28</druid.version>
        <bitwalker.version>1.21</bitwalker.version>
        <kaptcha.version>2.3.3</kaptcha.version>
        <pagehelper.boot.version>2.1.0</pagehelper.boot.version>
        <fastjson.version>2.0.57</fastjson.version>
        <oshi.version>6.8.2</oshi.version>
        <commons.io.version>2.19.0</commons.io.version>
        <poi.version>4.1.2</poi.version>
        <velocity.version>2.3</velocity.version>
        <jwt.version>0.9.1</jwt.version>
        <mybatis-plus-spring-boot.version>3.5.7</mybatis-plus-spring-boot.version>
        <mybatis.version>3.5.16</mybatis.version>
        <springdoc.version>2.8.14</springdoc.version>
        <jsqlparser.version>4.9</jsqlparser.version>
    </properties>

    <!-- 依赖声明 -->
    <dependencyManagement>
        <dependencies>

            <!-- SpringBoot的依赖配置-->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- 阿里数据库连接池 (Spring Boot 3.x) -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- 解析客户端操作系统、浏览器等 -->
            <dependency>
                <groupId>eu.bitwalker</groupId>
                <artifactId>UserAgentUtils</artifactId>
                <version>${bitwalker.version}</version>
            </dependency>

            <!-- pagehelper 分页插件 -->
            <dependency>
                <groupId>com.github.pagehelper</groupId>
                <artifactId>pagehelper-spring-boot-starter</artifactId>
                <version>${pagehelper.boot.version}</version>
            </dependency>

            <!-- jsqlparser (MyBatis-Plus 3.5.x 需要 4.x 版本) -->
            <dependency>
                <groupId>com.github.jsqlparser</groupId>
                <artifactId>jsqlparser</artifactId>
                <version>${jsqlparser.version}</version>
            </dependency>

            <!-- 获取系统信息 -->
            <dependency>
                <groupId>com.github.oshi</groupId>
                <artifactId>oshi-core</artifactId>
                <version>${oshi.version}</version>
            </dependency>

            <!-- spring-doc -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- io常用工具类 -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons.io.version}</version>
            </dependency>

            <!-- excel工具 -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- velocity代码生成使用模板 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>

            <!-- 阿里JSON解析器 -->
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson.version}</version>
            </dependency>

            <!-- Token生成与解析-->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt</artifactId>
                <version>${jwt.version}</version>
            </dependency>

            <!-- 验证码 -->
            <dependency>
                <groupId>pro.fessional</groupId>
                <artifactId>kaptcha</artifactId>
                <version>${kaptcha.version}</version>
            </dependency>

            <!-- 定时任务-->
            <dependency>
                <groupId>com.xhzb</groupId>
                <artifactId>xhzb-quartz</artifactId>
                <version>${xhzb.version}</version>
            </dependency>

            <!-- 代码生成-->
            <dependency>
                <groupId>com.xhzb</groupId>
                <artifactId>xhzb-generator</artifactId>
                <version>${xhzb.version}</version>
            </dependency>

            <!-- 核心模块-->
            <dependency>
                <groupId>com.xhzb</groupId>
                <artifactId>xhzb-framework</artifactId>
                <version>${xhzb.version}</version>
            </dependency>

            <!-- 系统模块-->
            <dependency>
                <groupId>com.xhzb</groupId>
                <artifactId>xhzb-system</artifactId>
                <version>${xhzb.version}</version>
            </dependency>

            <!-- 通用工具-->
            <dependency>
                <groupId>com.xhzb</groupId>
                <artifactId>xhzb-common</artifactId>
                <version>${xhzb.version}</version>
            </dependency>

            <!-- 护理平台-->
            <dependency>
                <groupId>com.xhzb</groupId>
                <artifactId>xhzb-nursing-platform</artifactId>
                <version>${xhzb.version}</version>
            </dependency>

            <!-- 护理平台-->
            <dependency>
                <groupId>com.xhzb</groupId>
                <artifactId>xhzb-oss</artifactId>
                <version>${xhzb.version}</version>
            </dependency>

            <dependency>
                <groupId>org.mybatis</groupId>
                <artifactId>mybatis</artifactId>
                <version>${mybatis.version}</version>
            </dependency>

            <!-- mybatis-plus 增强CRUD -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus-spring-boot.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-annotation</artifactId>
                <version>${mybatis-plus-spring-boot.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <modules>
        <module>xhzb-admin</module>
        <module>xhzb-framework</module>
        <module>xhzb-system</module>
        <module>xhzb-quartz</module>
        <module>xhzb-generator</module>
        <module>xhzb-common</module>
        <module>xhzb-nursing-platform</module>
        <module>xhzb-oss</module>
    </modules>
    <packaging>pom</packaging>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>