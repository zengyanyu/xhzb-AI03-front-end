# 2026-08-21

## Dify 智能体聊天集成
- 后端(xhzb-nursing-platform, 未改已有 /ai/** 代码):
  - `config/DifyProperties.java`: @ConfigurationProperties(prefix="dify"), 默认值 base-url=http://192.168.100.128/v1, api-key=app-TPBAg6vcHXbARqwUap0bnTwG
  - `service/DifyChatService.java` + `impl/DifyChatServiceImpl.java`: JDK HttpClient 调 Dify open-api; 流式聊天用 SseEmitter(专用线程池 dify-sse-forward) 转发 Dify SSE 事件; 会话列表/历史消息/删除会话为普通 JSON 请求; 日志前缀 [Dify][xxx] 便于排查
  - `controller/DifyChatController.java`: /dify/chat(POST,SSE)、/dify/conversations(GET)、/dify/messages(GET)、/dify/conversations/{id}(DELETE); user 参数取 SecurityUtils.getUsername()(异常时 default-user)
- 前端(xhzb_ui, 未改已有小智菜单):
  - `src/api/nursing/dify.js`: fetch 流式聊天 + request 封装其余接口
  - `src/views/nursing/dify/index.vue`: 流式聊天(SSE帧解析 event:/data:)、会话列表、历史消息(query/answer 展开为两条)、删除会话(确认框)
- 菜单: `sql/dify_menu.sql` 在星海智询(2052)下新增"dify小智", menu_id 自增避免与 AUTO_INCREMENT=2071 冲突
- 配置: xhzb-admin/application.yml 新增 dify 配置块(唯一修改的已有文件)
- 验证: `mvn -pl xhzb-nursing-platform -am compile` 通过; `vite build` 通过
- 注意: Git Bash 下 mvn 脚本报 classworlds 错误, 需用 PowerShell 调 mvn.cmd 且先设 JAVA_HOME=jdk-17
